{
  "name": "client-certificate-test-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "docker build . -t client-certificate-test:latest",
    "start": "docker run --rm -p 8080:3000 -p 8443:443 --env-file .env -e AWS_ACCESS_KEY_ID -e AWS_SECRET_ACCESS_KEY -e AWS_SESSION_TOKEN -e SECRET_ID -e SERVER_CERTIFICATE_ARN --name client-certificate-test client-certificate-test:latest",
    "stop": "docker kill client-certificate-test"
  },
  "dependencies": {
    "aws-sdk": "^2.660.0",
    "express": "^4.17.1"
  }
}
