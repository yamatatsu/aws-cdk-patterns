{
  "name": "aws-cdk-patterns",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "patterns/*"
  ],
  "scripts": {
    "test": "lerna run --stream test",
    "type-check": "lerna run --stream type-check",
    "ci": "run-p -ln test type-check"
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "npm-run-all": "4.1.5",
    "prettier": "2.2.1"
  }
}
